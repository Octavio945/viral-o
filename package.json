{
  "name": "viraleo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r --filter \"@viraleo/api\" --filter \"web\" dev",
    "build": "pnpm -r --filter \"@viraleo/api\" --filter \"web\" build",
    "lint": "pnpm -r --filter \"@viraleo/api\" --filter \"web\" lint",
    "type-check": "pnpm -r --filter \"@viraleo/api\" --filter \"web\" type-check",
    "kill-ports": "npx kill-port 3000 4000"
  }
}
